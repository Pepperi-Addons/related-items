{
	"name": "typescript",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "cd ./client-side && npm run build && cd .. && cd ./cpi-side && npm run build && cd .. && cd ./server-side && npm run build && cd ..",
		"init": "npm install && cd ./client-side && npm install --force && cd .. && cd ./cpi-side && npm install && cd .. && cd ./server-side && npm install && cd ..",
		"publish-addon": "npm run build && publish-addon",
		"cpi-publish-addon": "cd ./cpi-side && npm run build:debug && cd .. && cpi-node-debug"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@pepperi-addons/cpi-node": "^0.2.3",
		"@pepperi-addons/papi-sdk": "^1.47.3",
		"@pepperi-addons/scripts": "^2.0.13",
		"file-saver": "^2.0.5"
	}
}
